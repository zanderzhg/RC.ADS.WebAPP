@model RC.ADS.Data.Entity.AD_Account.AccountInfo
@using RC.ADS.Data.Enum
@{
    ViewData["Title"] = "充值";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2> ViewData["Title"]</h2>

<h4>AccountInfo</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="OwnerId" type="hidden" />
           
            <div class="form-group">
                <label asp-for="Money" class="control-label"></label>
                <input asp-for="Money" class="form-control" />
                <span asp-validation-for="Money" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AccountInfoChangeTpye" class="control-label"></label>
                <select name="AccountInfoChangeTpye" asp-items="@Html.GetEnumSelectList(typeof(AccountInfoChangeTypeEnum))"></select>
                 
                <span asp-validation-for="AccountInfoChangeTpye" class="text-danger"></span>

            </div>
            <div class="form-group">
                <label asp-for="Describe" class="control-label"></label>
                <input asp-for="Describe" class="form-control" />
                <span asp-validation-for="Describe" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="Menbers" asp-action="Index" >Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
