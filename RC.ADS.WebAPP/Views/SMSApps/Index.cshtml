@model IEnumerable<RC.ADS.Data.Entity.AD_SMS.SMSApp>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.appid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.appkey)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SMSAppName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Describe)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.appid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.appkey)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SMSAppName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Describe)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
